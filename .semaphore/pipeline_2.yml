version: v1.0
name: Pipeline 2
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Docker Build
    task:
      jobs:
        - name: Build
          commands:
            - cd ..
            - 'echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin'
            - 'docker pull $DOCKER_USERNAME/rtfm:latest || true'
            - 'docker build --cache-from=$DOCKER_USERNAME/rtfm:latest -t $DOCKER_USERNAME/rtfm:latest .'
            - 'docker push $DOCKER_USERNAME/rtfm:latest'
      secrets:
        - name: dockerhub
      prologue:
        commands:
          - sem-version python 3.9
          - checkout
          - cache restore
          - mkdir -p .pip_cache
          - pip install --cache-dir .pip_cache -r requirements.txt
          - cd rtfm
          - export FLASK_APP=rtfm.py
